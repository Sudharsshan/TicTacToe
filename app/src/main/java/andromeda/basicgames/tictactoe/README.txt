===============================README================================================
    Implement a new UI for the game arena which includes a grid layout with borders
            - Re-style the UI to implement a sketch-pad type arena
            - add sounds for every move made on the board
            - add a animated gif for showing the thoughts of bot (**Implement in later stages**)
            - Add a grid layout with visible border lines similar to grid of the game.
            - add a score history page to display previous matches along with date and time taken to win.
            - add time taken to win feature to show the duration of the match.